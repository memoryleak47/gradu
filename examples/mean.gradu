# mean.gradu

# This is hard as "a" and "b" have different type layouts if done improperly.
# This can easily become UB.

a = fn(x) { return x+1; };
b = fn(x) { return "ok"; };

_ = b("wow");

if true {
    f = a;
} else {
    f = b;
}

# OUT: 42
print(f(41));
