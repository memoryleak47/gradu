# global.gradu

counter = 0;

fn fresh_int() {
    global counter;

    out = counter;
    counter = counter + 1;
    return out;
}

# OUT: 0
x = fresh_int();
# OUT: 1
y = fresh_int();
# OUT: 2
z = fresh_int();
print(x);
print(y);
print(z);
